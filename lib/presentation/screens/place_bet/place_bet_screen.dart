import 'package:flutter/material.dart';

class PlaceBetScreen extends StatelessWidget {
  const PlaceBetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Place Bet Screen'),
      ),
    );
  }
}
