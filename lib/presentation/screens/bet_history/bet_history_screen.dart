import 'package:flutter/material.dart';

class BetHistoryScreen extends StatelessWidget {
  const BetHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Bet History Screen'),
      ),
    );
  }
}
